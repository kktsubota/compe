#include <algorithm>
#include <iostream>
#include <utility>
#include <map>
#include <vector>

using namespace std;


int main(int argc, char const *argv[]) {
    /* code */
    int n;
    cin >> n;
    vector<int> a(n);
    map<int, int> count;
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
    
    return 0;
}

